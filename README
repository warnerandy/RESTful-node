This module creates a restful api out of files and objects given the correct path and template in the RESTconfig.js file. This allows for the rapid development of serialized objects (currently only json)